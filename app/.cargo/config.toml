# Share the zed-upstream target dir so gpui's dependency artifacts compile once,
# not once per workspace. Remove when we move to a published gpui.
[build]
target-dir = "/home/pbrown/BROWN-FAMILY-SPORTS/Software/zed-upstream/target"
