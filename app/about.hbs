THIRD-PARTY LICENSES — terminal-delight
================================================================================

This binary statically links the Rust crates listed below, including the pinned
Zed/gpui workspace it is built from. terminal-delight's own source is MIT. Every
dependency here is used under a permissive (non-copyleft) license; the GPL crates
the Zed graph would otherwise pull in (ztracing, zlog) are removed at build time
by docs/patches/0002-sever-gpl-crates.patch, so nothing below is copyleft.

Generated by cargo-about from app/about.toml.

Summary
--------------------------------------------------------------------------------
{{#each overview}}
  {{count}}x  {{{name}}} ({{id}})
{{/each}}

Full license texts
--------------------------------------------------------------------------------
{{#each licenses}}
================================================================================
{{{name}}} ({{id}})

Used by:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}
{{/each}}
--------------------------------------------------------------------------------
{{{text}}}

{{/each}}
