node_modules/
dist/
.appimage-build/
.DS_Store
*.log
.vite/
app/target/
# machine-local cargo config (shared target dir); see docs/patches/ for the gpui setup
app/.cargo/config.toml
# local agent scratch: Claude Code settings + linked git worktrees
.claude/

# Local-only demo logo/art assets (never committed — populate the demo wall)
demo-assets/

# photographs from scripts/verify-site.mjs
site-shots/

# Python bytecode from plugin test runs (plugins/*/ are scripts, never compiled artifacts)
__pycache__/
*.pyc
