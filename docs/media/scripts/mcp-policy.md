# mcp-policy — the read-only stance, moving

> One line per card. Say them over the picture; the prompter walks them on the
> video's own clock, so keep each one to roughly a breath.

This terminal lets other agents watch it work.
Read only — it says so on the panel, in words.
Now it opens up: agents only, then every pane.
The write gate opens. They can restyle a pane.
They still cannot type a single byte into one.
Then it shuts again, because that is the default.
