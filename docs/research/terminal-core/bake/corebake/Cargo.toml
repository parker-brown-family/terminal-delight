[package]
name = "corebake"
version = "0.1.0"
edition = "2021"

[dependencies]
rio-vt = { version = "=0.5.28", default-features = false, features = ["graphics"] }
alacritty_terminal = "=0.26.0"
serde_json = "1"

[profile.release]
debug = false
