[package]
name = "probe"
version = "0.1.0"
edition = "2024"

[dependencies]
# The crate's own `png` feature only adds `RustPngDecoder`, which has no public
# constructor, so we decode PNGs ourselves with the `png` crate.
libghostty-vt = "=0.2.1"
png = "0.18"
serde_json = "1"
